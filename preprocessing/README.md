## Directory contains files to:
- Preprocess clinical data to standardize continuous values and other preprocessing
- Preprocess original CT volumes by reading CT volume, cropping them around chest region using TotalSegmentator, resampled to a common spatial resolution, windowing and normalization and finally saving them at a fixed target size
- Generating weak concepts for all datapoints using the predefined knowledge bank
