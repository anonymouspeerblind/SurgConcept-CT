## Directory contains:
- json file that lists all clinical features
- json file that lists all postoperative complications
- json file that provide feature name mapping for clinical features used during clinical feature tokenization
- json file that provide feature type mapping for clinical features used during clinical feature tokenization
- Global vocabulary used during clinical feature tokenization
- Knowledge Bank used to generate weak concepts of all datapoints
- ordinal values map for clinical features used during clinical feature tokenization
